#include <stdio.h>
#include <stdlib.h>

struct VA {
    // опишите структуру данных
};

unsigned int VA_to_PA(unsigned int VA) {
    // функция, получающая на вход виртуальный адрес и преобразующая его в физический
    return 1;
}
