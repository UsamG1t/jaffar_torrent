#include <stdio.h>
#include <stdlib.h>

typedef struct Seg {
} segment;

unsigned int VirtIntoPhys (segment *SegTable, unsigned int VirtAdr) {
    //виртуальный сегментный адрес VirtAdr, массив структур SegTable
    return 1;
}
